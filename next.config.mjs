/** @type {import('next').NextConfig} */
const nextConfig = {
  // Disable Tailwind and use Chakra UI instead
  // Server components need this to allow Chakra UI
  experimental: {},
};

export default nextConfig;
